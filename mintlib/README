This is the an old version of MiNTLib.

It uses a sligthly different build system (using settings from
CONFIGVARS int the top-level), and was modified to not accidently
install it as default library.
